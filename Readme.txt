This python script is to automate tasks consisting of:

  - clone public repository to a local machine and create the cloned repository to another GitHub account

  - automate github actions workflow run


Make sure to install gitpython